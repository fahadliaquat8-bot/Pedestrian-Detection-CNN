# Pedestrian-Detection-CNN
A deep learning-based project that detects and counts pedestrians in images using a pre-trained Faster R-CNN model. It highlights detected persons with bounding boxes and provides confidence scores for each detection.
